---
# tasks file for roles/proxy
- name: Run Proxy
  docker_container:
    name: proxy
    image: hipp0x/devops-server:latest
    state: started
    networks:
      - name: "app-network"
    ports:
      - "80:80"